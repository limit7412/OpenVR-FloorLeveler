<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>WinExe</OutputType>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
    <AvaloniaUseCompiledBindingsByDefault>true</AvaloniaUseCompiledBindingsByDefault>
    <AssemblyName>FloorLeveler</AssemblyName>
    <DebugType>embedded</DebugType>
    <!-- 単一 exe 発行 (仕様 §8.2) の対象 RID。発行は README の手順を参照。 -->
    <RuntimeIdentifiers>win-x64</RuntimeIdentifiers>
    <!-- タグ駆動バージョン (仕様 §8.4)。リリースは p:Version にタグの値を渡して発行する。
         コミットハッシュを InformationalVersion に混ぜると exe のファイルプロパティ
         (ProductVersion) やバージョン出力がタグと一致しなくなるため抑止する。 -->
    <IncludeSourceRevisionInInformationalVersion>false</IncludeSourceRevisionInInformationalVersion>
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="..\FloorLeveler.Core\FloorLeveler.Core.csproj" />
    <ProjectReference Include="..\FloorLeveler.OpenVr\FloorLeveler.OpenVr.csproj" />
  </ItemGroup>

  <ItemGroup>
    <PackageReference Include="Avalonia" Version="12.1.0" />
    <PackageReference Include="Avalonia.Desktop" Version="12.1.0" />
    <PackageReference Include="Avalonia.Fonts.Inter" Version="12.1.0" />
    <PackageReference Include="Avalonia.Themes.Fluent" Version="12.1.0" />
  </ItemGroup>

</Project>
